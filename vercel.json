# See https://vercel.com/docs/cron-jobs
crons:
  - path: /api/cron/cleanup
    schedule: "5 6 * * *"
  # Hourly reconcile: derives backlog implicitly; notifies cache + optional purge prep
  - path: /api/cron/backlog-reconcile
    schedule: "5 * * * *"
